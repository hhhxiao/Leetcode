#include <set>
#include <unordered_map>
#include <vector>

using namespace std;
#include "dbg.h"
#include "io/io.h"
#include "template/prints.h"

int minimizeMax(vector<int>& nums, int p) {
    std::sort(nums.begin(), nums.end());
    // 排序，找最小
}

int main() {
    std::vector<int> r{0, 5, 3};

    return 0;
}
