#include <iostream>
#include <vector>

#include "dbg.h"

using namespace std;

int maxProduct(vector<int>& nums) {
    // -2 3 1 -3 4 -1
    //        -3 4 -1
    return 0;
}
int main(int argc, char const* argv[]) {
    std::vector<int> v = {2, 3, -2, 4};
    maxProduct(v);
    return 0;
}
