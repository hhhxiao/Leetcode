#include <vector>
using namespace std;

void sort() {}

bool hasValidPath(vector<vector<int>>& grid) {
    const int row = grid.size();
    const int col = grid[0].size();
    int curX = 0;
    int curY = 0;

    while (!(curX == row - 1 && curY == col - 1)) {
        /* code */
    }
}
